<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gpriatmaja.pulsaku">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mpulsa"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Pulsa" />
        <activity android:name=".Paketan" />
        <activity android:name=".Listrik" />
        <activity android:name=".Laporan" />
        <activity android:name=".DetailLaporan" />
        <activity android:name=".Laporan2" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>
